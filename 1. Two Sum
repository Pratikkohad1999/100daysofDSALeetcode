class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        map<int,int> n;
        vector<int> a;
        for(int j=0;j<nums.size();j++)
        {
            if(n.find(target-nums[j])!=n.end())
            {
                a.push_back(n[target-nums[j]]);
                a.push_back(j);
                return a;
            }
            n[nums[j]]=j;
        }
        return a;
    }
};
